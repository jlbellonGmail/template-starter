# Audit Profile — APPLICATION

**Estado:** NO IMPLEMENTADO

Este perfil está reservado para futuras auditorías de aplicaciones.

Mientras su estado sea `NO IMPLEMENTADO`:

* no debe utilizarse para puntuar proyectos;
* no debe cargarse durante auditorías realizadas con otro perfil;
* no modifica `QUALITY_SCORE.md`;
* no modifica `AUDIT_RULES.md`;
* no modifica el perfil activo.

Para auditorías `TEMPLATE`, este archivo debe ignorarse completamente.

Su contenido se definirá y versionará antes de realizar la primera auditoría de tipo `APPLICATION`.
